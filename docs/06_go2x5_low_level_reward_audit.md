# Go2-X5 Low-Level Reward Audit

This report is generated by:

- `low-level/legged_gym/scripts/audit_go2x5_low_level_rewards.py`

Run it from the repository root with:

```bash
python3 low-level/legged_gym/scripts/audit_go2x5_low_level_rewards.py --output docs/06_go2x5_low_level_reward_audit.md
```

## Static Context

- Config: `low-level/legged_gym/envs/manip_loco/go2x5_config.py`
- Reward implementation: `low-level/legged_gym/envs/rewards/maniploco_rewards.py`
- Env reward aggregation: `low-level/legged_gym/envs/manip_loco/manip_loco.py:398`
- PPO two-channel reward storage: `third_party/rsl_rl/rsl_rl/algorithms/ppo.py:129`, `third_party/rsl_rl/rsl_rl/storage/rollout_storage.py:56`
- only_positive_rewards: `True`
- observe_gait_commands: `False`
- base_height_target: `0.32`
- tracking_sigma: `0.05`
- tracking_ee_sigma: `0.15`
- collision_force_threshold: `5.0`
- collision_soft_clip: `50.0`
- feet_height_target: `0.12`
- max_contact_force: `200.0`
- Active leg reward terms: `11`
- Active arm reward terms: `4`
- Reward implementations with reviewed metadata: `75/75`
- PPO policy num_leg_actions: `12`
- PPO policy num_arm_actions: `0`
- PPO reward mixing_schedule: `[1.0, 3000, 3000]`

Reward aggregation details:

- Leg rewards are summed into `rew_buf` and divided by 100.
- Arm rewards are summed into `arm_rew_buf` and divided by 100.
- Episode reward summaries remain per-second totals; `Episode_metric/*` summaries are raw per-policy-step means so curriculum thresholds keep their physical units.
- PPO stores `[rew_buf, arm_rew_buf]` as a two-channel reward/value/advantage signal.
- In the current low-level config `num_arm_actions=0`, so the arm channel has no independent arm-action log-prob gradient. It affects the 12D leg policy through `mixing_advantages_batch[...,0] = leg_adv + value_mixing_ratio * arm_adv`; `mixing_schedule=[1.0, 3000, 3000]` keeps iterations 0--3000 locomotion-only, then ramps the ratio from 0 to 1 during iterations 3000--6000.

## Go2-X5 Order And Body Resolution

- URDF movable joint order: `FL_hip_joint, FL_thigh_joint, FL_calf_joint, FR_hip_joint, FR_thigh_joint, FR_calf_joint, RL_hip_joint, RL_thigh_joint, RL_calf_joint, RR_hip_joint, RR_thigh_joint, RR_calf_joint, arm_joint1, arm_joint2, arm_joint3, arm_joint4, arm_joint5, arm_joint6, arm_joint7, arm_joint8`
- Policy leg joint order: `FR_hip_joint, FR_thigh_joint, FR_calf_joint, FL_hip_joint, FL_thigh_joint, FL_calf_joint, RR_hip_joint, RR_thigh_joint, RR_calf_joint, RL_hip_joint, RL_thigh_joint, RL_calf_joint`
- Runtime foot sensor/reward order: `FL_foot, FR_foot, RL_foot, RR_foot`
- Policy-visible foot observation order after `_reindex_feet`: `FR_foot, FL_foot, RR_foot, RL_foot`
- EE body: `arm_eef_link`
- Penalized contact tokens: `['base', 'Head', 'hip', 'thigh', 'calf', 'arm_link']`
- Resolved penalized contact bodies: `base, arm_base_link, Head_upper, Head_lower, FL_hip, FL_hip_rotor, FR_hip, FR_hip_rotor, RL_hip, RL_hip_rotor, RR_hip, RR_hip_rotor, FL_thigh, FL_thigh_rotor, FR_thigh, FR_thigh_rotor, RL_thigh, RL_thigh_rotor, RR_thigh, RR_thigh_rotor, ... (44 total)`
- Resolved termination contact bodies: `none`

## Main Findings

1. Every reward implementation has reviewed semantics, and no active reward sign mismatch was found.
2. No named gait is prescribed: gait-clock observations, contact-phase shaping, swing-height shaping, and walking-posture shaping are disabled.
3. One 10-second command is held for the whole episode: 10% exact standing, 50% exact bidirectional straight walking, 10% non-dead-zone in-place turning, and 30% general x/y/yaw motion.
4. Locomotion uses a bounded static-baseline-normalized x/y and yaw kernel: moving commands earn 0 at rest and 1 at target. Completed-landings add a phase-free air-time/clearance bonus and contacting horizontal slip is penalized; no gait phase, trot, or four-beat walk is prescribed.
5. `tracking_ee_world` and quaternion `tracking_ee_orn` use `arm_eef_link` as active raw PPO channels. They are not multiplied by a support gate, so coordinated crouching can improve low terrain-fixed reach targets.
6. `collision` is fail-visible for base, head, non-foot leg, arm, wrist, and finger contacts; the four feet remain outside this penalty.
7. `tracking_contacts_shaped_vel` reads the freshly refreshed rigid-body tensor directly; the advanced-indexed foot cache is refreshed each policy tick and checked independently.
8. Auto curriculum is disabled. The task distribution stays flat and static, while physical arm motion alone is held until iteration 3000 so locomotion is acquired under a stationary front payload before whole-body reward mixing begins.

## Active Reward Audit Table

| term | channel | scale | raw meaning | expected sign | sign check | source | dependency | Go2-X5 migration risk | verification |
|---|---:|---:|---|---:|---|---|---|---|---|
| `feet_air_time` | leg | 1.0 | sum((clamp(air_time - 0.10, 0, 0.25) + bounded swing-clearance bonus) on completed landing) | + | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:698` | force_sensor_tensor order, per-foot contact timing, terrain-relative swing peak height | Short landings must remain neutral, permanently airborne feet must earn zero, and all four feet must use the same phase-free formula. | Touch each foot independently; verify no pre-landing reward, no negative short-step reward, bounded long-step reward, and state reset on contact. |
| `tracking_lin_vel` | leg | 2.0 | standing: exp(-xy_error^2/sigma); moving xy: clamp((kernel-static_kernel)/(1-static_kernel), -1, 1); inactive xy while turning: kernel-1 | + | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:165` | commands[:,0:2] and base_lin_vel[:,0:2] | Go2-X5 samples a small lateral range, so command and observation order must remain x,y,yaw. | For a nonzero xy command verify reward 0 at rest, 1 at target, and negative for wrong-way motion; stopped commands retain the stability kernel. |
| `tracking_ang_vel` | leg | 0.5 | standing: exp(-yaw_error^2/sigma); moving yaw: clamp((kernel-static_kernel)/(1-static_kernel), -1, 1); inactive yaw while moving: kernel-1 | + | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:263` | commands[:,2], base_ang_vel[:,2] | Sign is correct; confirm command yaw is not confused with vertical linear velocity in any caller. | For a nonzero yaw command verify reward 0 at rest, 1 at target, and negative for wrong-way rotation; stopped commands retain the stability kernel. |
| `torques` | leg | -2.5e-05 | sum(square(all torques)) | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:239` | torques tensor, leg plus zero arm/gripper torque convention | Sign is correct; with low-level 12D actions arm torques should not dominate this term. | Check per-joint torque contribution; arm/gripper entries should be zero or intentionally excluded. |
| `lin_vel_z` | leg | -1.0 | square(base_lin_vel_z) | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:255` | base linear velocity in base frame | Sign is correct; excessive vertical oscillation should be penalized. | Inject upward/downward base z velocity; weighted reward should become more negative. |
| `roll` | leg | -2.0 | abs(base roll) | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:497` | base_quat to roll extraction | Sign is correct; pitch is not directly penalized by this active term. | Probe roll {0, 0.2, 0.5}; weighted reward should monotonically decrease. |
| `collision` | leg | -1.0 | sum(clamp(norm(contact_force on penalized bodies)-threshold, 0, soft_clip)/threshold) | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:310` | penalized_contact_indices from asset.penalize_contacts_on substrings | The resolved set must include base/head/leg and all arm/finger bodies while excluding the four feet. | Print resolved penalized body names; manually create base/head/leg/arm/finger contacts and confirm each intended body counts. |
| `action_rate` | leg | -0.01 | sum(square(last_actions - actions) over leg actions) | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:296` | policy action order before/after _reindex_all | Sign is correct; action-order bugs can still hide behind a smooth but wrong action stream. | Pulse policy FR hip action and confirm the smoothness term uses the policy-order action history consistently. |
| `dof_pos_limits` | leg | -10.0 | sum soft joint-limit violation over leg joints | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:300` | dof_pos_limits, URDF leg dof order | Sign is correct; verify Go2-X5 URDF limits are not inherited from B1/Z1 or too tight after mimic edits. | Sweep each leg joint near lower/upper limit; raw should be zero inside and positive outside soft range. |
| `feet_drag` | leg | -0.2 | sum squared horizontal foot speed for filtered contacting feet | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:367` | feet_indices, filtered contacts, and live horizontal foot velocity | Vertical touchdown speed must not be mislabeled as drag; foot and contact order must both be FL,FR,RL,RR. | Slide one contacting foot horizontally; vertical-only or airborne motion must contribute zero. |
| `feet_contact_standing` | leg | -0.5 | number of feet off ground while the command is stopped | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:389` | force-sensor contact booleans and walking command mask | A bad contact threshold can penalize a nominal four-foot stance. | At zero command lift one foot at a time; the raw penalty should rise by one and be zero while walking. |
| `tracking_ee_world` | arm | 2.0 | exp(-2 * L1(ee_pos - curr_ee_goal_cart_world) / tracking_ee_sigma) | + | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:17` | gripper_idx=arm_eef_link, ee_pos rigid body state, world-frame EE goal | Sign is correct; with num_arm_actions=0 it influences the leg policy through PPO reward mixing, not a separate arm action head. | Set goal exactly at arm_eef_link and then offset x/y/z; raw should be highest at zero offset and decay monotonically. |
| `tracking_ee_orn` | arm | 0.6 | exp(-quaternion angular error / tracking_ee_orientation_sigma) | + | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:105` | ee_goal_orn_quat, normalized ee_orn, arm_eef_link grasp direction | The X5 +x approach axis and quaternion sign invariance must not be confused with the legacy Z1 Euler convention. | Sweep equivalent quaternion signs and bounded X5 local RPY targets; reward must peak at the same physical pose. |
| `height_adaptation` | arm | -3.0 | absolute base-height error from a 0.22--0.32 m target interpolated over EE goal z 0.05--0.30 m | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:512` | terrain-relative EE goal z and base height | The crouch target must remain above termination height and use terrain-relative coordinates. | Probe goal z below/inside/above the anchors; target height must be 0.22/interpolated/0.32 m. |
| `pitch_adaptation` | arm | -1.0 | absolute base-pitch error from a 0.25--0.00 rad target interpolated over EE goal z 0.05--0.30 m | - | OK | `low-level/legged_gym/envs/rewards/maniploco_rewards.py:519` | terrain-relative EE goal z and base pitch | The pitch sign must lower the front/arm mount and remain well inside the termination threshold. | Probe low/high goal z; target pitch must be +0.25/0.00 rad and the error must vanish at the target. |

## Curriculum Reward Overrides

| stage | term | scale | expected sign | sign check |
|---|---|---:|---:|---|

## Disabled But Migration-Relevant Terms

| term | configured value | why it still matters | verification before enabling |
|---|---:|---|---|
| `tracking_contacts_shaped_force` | 0.0 | When enabled, the coefficient must remain positive or bad off-phase contact becomes positive reward. | Enable observe_gait_commands in a small probe; inject off-phase foot force and confirm weighted reward decreases. |
| `tracking_contacts_shaped_vel` | 0.0 | When enabled, the coefficient must remain positive and must not read an advanced-indexed cache that is never refreshed. | Inject stance-foot velocity into live rigid_body_state, refresh the cache, and confirm both the raw reward and cache value change. |
| `feet_height` | 0.0 | Each desired-swing foot must clear independently; one high foot must not mask another low foot. | Sweep each foot z independently; only the low desired-swing foot should contribute clearance error. |
| `tracking_lin_vel_max` | 0.0 | This progress-only reward must remain disabled because overspeed saturates at the optimum. | Probe command vx {-0.5,0,0.5} and base vx offsets; weighted reward should be highest near command. |
| `tracking_lin_vel_x_l1` | 0.0 | Near-zero command normalization is sensitive to the stop threshold. | Probe positive, negative, and stopped commands. |
| `tracking_lin_vel_x_exp` | 0.0 | This compatibility term is disabled for Go2-X5 in favor of the squared XY tracking term. | Sweep equal under- and overspeed errors; rewards must match and peak only at exact tracking. |
| `tracking_ang_vel_yaw_exp` | 0.0 | Yaw must not be confused with vertical linear velocity. | Sweep yaw-rate error monotonically. |
| `stand_still` | 0.0 | Sign is correct; too much weight can oppose crouching needed for low EE goals. | With zero command, perturb leg default pose and confirm reward is highest at default. |
| `walking_dof` | 0.0 | It biases motion toward the default crouch and can recreate a no-step local optimum. | Keep it disabled unless an explicit ablation proves it does not suppress useful locomotion amplitude. |
| `dof_default_pos` | 0.0 | Enabling it during gait can recreate the no-step default-pose optimum. | Confirm maximum at the default pose and keep disabled for locomotion without an ablation. |
| `dof_error` | 0.0 | Wrong DOF order penalizes the wrong joints. | Perturb one named leg joint and confirm a positive quadratic error. |
| `alive` | 0.0 | Sign is correct; this is a survival baseline, not a behavior-specific signal. | Confirm the term is constant and termination penalties are handled separately. |
| `termination` | 0.0 | A wrong sign rewards falls; counting timeouts would also punish successful full episodes. | Trigger roll, pitch, height, and timeout resets; only non-timeout resets must receive the penalty. |
| `ang_vel_xy` | 0.0 | Sign is correct; complements roll but does not replace pitch/orientation checks. | Inject roll/pitch angular velocity; weighted reward should become more negative. |
| `dof_acc` | 0.0 | Sign is correct; confirms smoothness, not migration correctness by itself. | Apply a velocity jump to one leg joint and confirm the expected joint contribution increases. |
| `delta_torques` | 0.0 | Sign is correct; magnitude can hide action-order bugs if torques are assigned to wrong legs. | Apply a one-joint action impulse and confirm only the expected URDF leg torque changes. |
| `hip_pos` | 0.0 | Sign is correct; name lookup avoids relying on URDF order, but default angles must match Go2-X5. | Print hip_indices and dof_names; perturb each hip and check only that hip contributes. |
| `work` | 0.0 | Sign is correct; can penalize useful work as well as waste. | Probe zero velocity, co-directed torque/velocity, and opposing torque/velocity; magnitude should increase away from zero. |
| `feet_jerk` | 0.0 | Sign is correct; noisy force sensors on rough terrain can inject high-variance penalty. | Log raw term on flat standing and rough stepping; it should not dominate early reward breakdown. |
| `foot_lateral_spacing` | 0.0 | An incorrect foot order or side sign would reward crossed legs. | Move each foot toward and across the sagittal centerline; only the corresponding shortfall should increase. |
| `feet_contact_forces` | 0.0 | Sign is correct; max_contact_force=200 makes it a high-force limiter, not normal contact shaping. | Inspect force histograms; raw should be near zero for nominal stance and positive for impacts. |
| `low_goal_front_leg_bend` | 0.0 | Wrong leg indices or absolute world z shape the wrong posture. | Lower the EE goal and bend each front/rear leg independently. |
| `low_goal_posture_asymmetry` | 0.0 | Incorrect joint signs reward the opposite posture. | Compare front-only, rear-only, and symmetric crouches. |
| `low_goal_hind_leg_extension` | 0.0 | Wrong indices reward hind-leg crouch instead of extension. | Lower the goal and perturb RL/RR thigh/calf joints independently. |
| `low_goal_hind_support_force` | 0.0 | Foot-order errors swap front and hind support. | Inject front/hind forces independently and verify the ratio. |
| `hind_feet_contact_standing` | 0.0 | A migrated foot order could constrain the front pair instead of the rear support pair. | Lift FL, FR, RL, RR independently; only RL/RR should contribute while stopped. |
| `foot_support_standing` | 0.0 | A flickering contact threshold can create a high-variance standing penalty. | Sweep contact count 4,3,2,1 at zero command; raw penalty should be 0,0,1,2. |
| `pitch_soft_limit_standing` | 0.0 | Too tight a limit prevents body compensation needed for low EE targets. | Sweep pitch below and above 0.35 rad; penalty must be zero below it and disabled while walking. |
| `orientation` | 0.0 | This base penalty is independent of the position-only EE orientation setting. | Sweep base roll/pitch around identity; the minimum must occur at level orientation. |
| `orientation_walking` | 0.0 | Mask inversion penalizes the wrong mode. | Probe tilted base in stopped and walking modes. |
| `orientation_standing` | 0.0 | Mask inversion penalizes the wrong mode. | Probe tilted base in stopped and walking modes. |
| `base_height` | 0.0 | Sign is correct for target 0.32; terrain height sampling must be valid under Go2-X5 footprint. | Probe flat base z 0.24/0.32/0.41; weighted reward should be best at 0.32. |
| `stability_safety` | 0.0 | Any contact-count gate can favor one support pattern and suppress otherwise valid emergent locomotion. | Keep the term disabled in the simple profile; if restored, test valid walk, trot, and transition contacts independently. |
| `dof_error_deadzone` | 0.0 | Wrong default angles or leg slicing penalize the intended nominal stance. | Perturb each leg joint within and beyond the dead zone; only excess displacement should contribute. |
| `leg_action_l2_deadzone` | 0.0 | An overly small dead zone can suppress useful corrective actions during early learning. | Sweep one action around the dead zone; penalty must be zero inside and quadratic outside. |
| `torques_walking` | 0.0 | Incorrect wrapper dispatch or mask makes it unsafe to enable. | Call directly in both command modes. |
| `torques_standing` | 0.0 | Incorrect wrapper dispatch or mask makes it unsafe to enable. | Call directly in both command modes. |
| `energy_square` | 0.0 | Large scale can suppress useful gait motion. | Check zero at zero torque or velocity and quadratic growth otherwise. |
| `energy_square_walking` | 0.0 | Incorrect wrapper dispatch or mask makes the disabled term unsafe to enable. | Call the wrapper directly for stopped and walking commands. |
| `energy_square_standing` | 0.0 | Incorrect wrapper dispatch or mask makes the disabled term unsafe to enable. | Call the wrapper directly for stopped and walking commands. |
| `base_height_walking` | 0.0 | Mask inversion would constrain standing instead of walking. | Check zero/nonzero commands at target and offset heights. |
| `base_height_standing` | 0.0 | Mask inversion would constrain walking instead of standing. | Check zero/nonzero commands at target and offset heights. |
| `penalty_lin_vel_y` | 0.0 | Suppressing it during every turn can permit lateral drift. | Probe lateral velocity with zero and nonzero yaw commands. |
| `tracking_ee_sphere` | 0.0 | Cartesian-mode Go2-X5 does not normally activate this legacy path. | Set an exact spherical target and offset each coordinate. |
| `tracking_ee_world_stable` | 0.0 | The support/height multiplier can zero the coordination signal precisely when crouching or stepping is required. | Keep it disabled in the simple profile and use raw world-position EE tracking for body-arm coordination. |
| `tracking_ee_sphere_walking` | 0.0 | Wrapper must call the reward container, not a nonexistent env method. | Call directly in both command modes. |
| `tracking_ee_sphere_standing` | 0.0 | Wrapper must call the reward container, not a nonexistent env method. | Call directly in both command modes. |
| `tracking_ee_cart` | None | Frame mismatch can reward the wrong world target. | Translate/yaw the base and reconstruct the expected world target independently. |
| `arm_energy_abs_sum` | None | A zero-gripper slice or wrong arm boundary can silently return an empty tensor. | Inject one arm torque/velocity pair and confirm a positive finite per-environment penalty. |
| `tracking_ee_orn_ry` | None | Euler wrapping and axis selection must match the end-effector convention. | Sweep wrapped roll/yaw errors around +/-pi. |

## Required Runtime Probes

Static analysis can verify sign consistency and dependency wiring, but it cannot prove that Isaac Gym rigid-body/contact tensors have the expected values at runtime. Before launching a long low-level run, run these probes:

1. Emergent-locomotion oracle: gait clocks must remain absent, contact-phase rewards must remain zero, and nonzero velocity commands must produce finite observations and rewards.
2. Adaptive-height monotonicity: sweep terrain-relative EE goal z across `0.05--0.30 m`; the body target must increase monotonically from `0.22` to `0.32 m` and remain above termination height.
3. Contact identity: touch `FL_foot, FR_foot, RL_foot, RR_foot` one at a time and confirm `force_sensor_tensor` order is `FL,FR,RL,RR`, while policy observation order is `FR,FL,RR,RL`.
4. Collision identity: create contact on a thigh, calf, base, head, arm link, and finger link; every non-foot contact should affect current `collision`.
5. EE position monotonicity: set `curr_ee_goal_cart_world` equal to `arm_eef_link` position, then offset x/y/z; `tracking_ee_world` raw value must decay monotonically.
6. Full-pose IK oracle: compare the 6-D weighted DLS update against an independent matrix solve and require orientation changes to alter arm targets.
7. All-function contract: call every `_reward_*` implementation and require a finite `(num_envs,)` raw tensor and metric tensor.

## Interpretation

The current Go2-X5 low-level reward set is sign-consistent and all reward implementations have reviewed metadata.
The static contract rejects any reintroduction of a named gait, an extra curriculum stage, or overlapping reward shaping.
Static signs do not prove simulator tensor identity, terrain-relative height semantics, or reset behavior.
Runtime acceptance therefore also requires the listed monotonicity and identity probes; their executed status is recorded in the dated training-readiness report.
